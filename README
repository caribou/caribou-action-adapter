# Interface Action Adapter-----------

Adapter utility for interface actions. Currently supports jython object
coercion and jython -> clojure map conversion. 

## Setup ----------------------------

mvn clean install

## Usage ----------------------------

objectFactory = new JythonObjectFactory(TargetInterface.class, "pythonModuleName");
jythonObject = (TargetInterface) objectFactory.createObject();
jythonObject.foo();